# Spec Tasks

## Tasks
- [x] 1. **Implement Team Member Admin CRUD**
  - [x] 1.1 Write feature tests for the admin `TeamMemberController` (index, store, update, destroy actions).
  - [x] 1.2 Create `Admin/TeamMemberController` with empty resource methods.
  - [x] 1.3 Register the `admin.teams` resource route in `routes/admin.php`.
  - [x] 1.4 Implement the `index`, `store`, `update`, and `destroy` methods in the controller, including validation.
  - [x] 1.5 Create the `resources/js/pages/Admin/Team/Index.tsx` component, copying the structure from the Categories page (table view + modal form).
  - [x] 1.6 Add a navigation link for "Team" in the admin sidebar layout.
  - [x] 1.7 Verify all feature tests pass and the UI is functional.

- [x] 2. **Implement Testimonial Admin CRUD**
  - [x] 2.1 Write feature tests for the admin `TestimonialController`.
  - [x] 2.2 Create `Admin/TestimonialController`.
  - [x] 2.3 Register the `admin.testimonials` resource route.
  - [x] 2.4 Implement the controller methods with validation.
  - [x] 2.5 Create the `resources/js/pages/Admin/Testimonials/Index.tsx` component, adapting it from the Team Member page.
  - [x] 2.6 Add a navigation link for "Testimonials" in the admin sidebar.
  - [x] 2.7 Verify all feature tests pass.

- [x] 3. **Implement Client Admin CRUD**
  - [x] 3.1 Write feature tests for the admin `ClientController`.
  - [x] 3.2 Create `Admin/ClientController`.
  - [x] 3.3 Register the `admin.clients` resource route.
  - [x] 3.4 Implement the controller methods with validation.
  - [x] 3.5 Create the `resources/js/pages/Admin/Clients/Index.tsx` component, adapting it from the previous pages.
  - [x] 3.6 Add a navigation link for "Clients" in the admin sidebar.
  - [x] 3.7 Verify all feature tests pass.
