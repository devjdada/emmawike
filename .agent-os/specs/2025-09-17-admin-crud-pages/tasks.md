# Spec Tasks

## Tasks
- [ ] 1. **Implement Team Member Admin CRUD**
  - [ ] 1.1 Write feature tests for the admin `TeamMemberController` (index, store, update, destroy actions).
  - [ ] 1.2 Create `Admin/TeamMemberController` with empty resource methods.
  - [ ] 1.3 Register the `admin.teams` resource route in `routes/admin.php`.
  - [ ] 1.4 Implement the `index`, `store`, `update`, and `destroy` methods in the controller, including validation.
  - [ ] 1.5 Create the `resources/js/pages/Admin/Team/Index.tsx` component, copying the structure from the Categories page (table view + modal form).
  - [ ] 1.6 Add a navigation link for "Team" in the admin sidebar layout.
  - [ ] 1.7 Verify all feature tests pass and the UI is functional.

- [ ] 2. **Implement Testimonial Admin CRUD**
  - [ ] 2.1 Write feature tests for the admin `TestimonialController`.
  - [ ] 2.2 Create `Admin/TestimonialController`.
  - [ ] 2.3 Register the `admin.testimonials` resource route.
  - [ ] 2.4 Implement the controller methods with validation.
  - [ ] 2.5 Create the `resources/js/pages/Admin/Testimonials/Index.tsx` component, adapting it from the Team Member page.
  - [ ] 2.6 Add a navigation link for "Testimonials" in the admin sidebar.
  - [ ] 2.7 Verify all feature tests pass.

- [ ] 3. **Implement Client Admin CRUD**
  - [ ] 3.1 Write feature tests for the admin `ClientController`.
  - [ ] 3.2 Create `Admin/ClientController`.
  - [ ] 3.3 Register the `admin.clients` resource route.
  - [ ] 3.4 Implement the controller methods with validation.
  - [ ] 3.5 Create the `resources/js/pages/Admin/Clients/Index.tsx` component, adapting it from the previous pages.
  - [ ] 3.6 Add a navigation link for "Clients" in the admin sidebar.
  - [ ] 3.7 Verify all feature tests pass.
